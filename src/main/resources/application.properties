# application properties (do NOT commit secrets)
spring.application.name=programm

spring.datasource.url=jdbc:mysql://localhost:3306/radbrain
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Email (use environment variables for secrets)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=radbrain.dynamik@gmail.com
spring.mail.password=lwziilovdxnxeizl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT config (do NOT commit secret to VCS; override via ENV)
jwt.secret=JpMQ/BA8Bbj0XacBnbyP/D7SvwVzrT9q/eIwbzyRvhQ
jwt.expiration-ms=86400000

# debug to see SMTP handshake in logs (enable only for troubleshooting)
#spring.mail.properties.mail.debug=${SPRING_MAIL_DEBUG:false}

# App email from address (non-secret)
#app.mail.from=${APP_MAIL_FROM:no-reply@radbrain.example}